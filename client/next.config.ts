import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['media.istockphoto.com', 'encrypted-tbn0.gstatic.com' , "img.daisyui.com"],
  },
};

export default nextConfig;
